#12
Easy startup guide:

Windows venv activation:
lifeTrackerVenv\Scripts\activate.bat

Linux and MacOS venv activation:
$ source lifeTrackerVenv/bin/activate

On your first activation of the virtual environment, run the following command line (all operating systems):
pip install -r requirements.txt

Deactivating (all operating systems):
deactivate